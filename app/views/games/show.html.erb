<div class="w-full max-w-md mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="font-bold text-xl text-center"><%= @game.title %></h1>
  </div>

  <div class="text-center">
    <%= link_to "New session", new_game_game_session_path(@game), class: "btn btn-primary" %>
  </div>
</div>
