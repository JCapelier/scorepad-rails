<div class="w-full max-w-md mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="font-bold text-xl text-center"><%= @game.title %></h1>
  </div>

<%= link_to "New session", new_game_session_path(game_id: @game.id), class: "btn bg-purple-600 hover:bg-purple-700 text-white font-semibold" %>

</div>
